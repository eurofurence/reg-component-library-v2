import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks"

import Footer from "../layout/footer"

<Meta title="Layout/Footer" component={Footer}/>

# Footer

The Footer component provides a surface at the bottom of a page to contain navigation elements.
It currently does not expose any parameters.

The Footer component wraps itself in a HTML `footer` tag.

## Baseline

<Canvas>
	<Story name="Baseline">
		<Footer/>
	</Story>
</Canvas>
